{
  "name": "@farfield/server",
  "version": "0.2.5",
  "private": false,
  "type": "module",
  "description": "Farfield server for Codex and OpenCode",
  "bin": {
    "farfield-server": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "dev:remote": "HOST=0.0.0.0 PORT=4311 tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "build:bundle": "rm -rf dist && esbuild src/index.ts --bundle --platform=node --format=esm --target=node20 --outfile=dist/index.js --banner:js='#!/usr/bin/env node\nimport { createRequire } from \"node:module\";\nconst require = createRequire(import.meta.url);' --external:pino --external:zod --external:@opencode-ai/sdk --external:@opencode-ai/sdk/* && chmod +x dist/index.js",
    "prepack": "bun run build:bundle",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@opencode-ai/sdk": "^1.2.6",
    "pino": "^9.5.0",
    "socket.io": "^4.8.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@farfield/api": "workspace:*",
    "@farfield/opencode-api": "workspace:*",
    "@farfield/protocol": "workspace:*",
    "@farfield/unified-surface": "workspace:*",
    "esbuild": "^0.25.10",
    "socket.io-client": "^4.8.1"
  }
}
